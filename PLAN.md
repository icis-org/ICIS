i have a plan. ICIS. Isam's Configurable Install system. It is an aio installer/setup. This installs the portable apps and it doesnt needs compile. its very easy. just appname.ici. the logic: it downloads the zip and extracts in the appdata or program files then add shortcut, startup and uninstaller.
ok but sometimes te zip links redirects like github.
so. the go is better than c#, rust, python, node, nsis. and its an universal. i make the app once and it works life. and i dont need repo url. bcuz i will just add latest in the link. users just installs ICIS and theres apps names then they select and it installs.

Double clicking an .ici file opens icis wizard.
It saves all install files in db so uninstall possible.
It can install multiple exes from one zip/rar/7z
It is gui first and .ici is easy sonfigurable script.